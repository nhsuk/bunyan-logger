{
  "name": "nhsuk-bunyan-logger",
  "version": "1.2.0",
  "description": "Facade over bunyan logger.",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "test": "NODE_ENV=test mocha --recursive test",
    "istanbul": "NODE_ENV=test istanbul",
    "generate-coverage": "yarn run istanbul -- cover _mocha -- --recursive",
    "check-coverage": "istanbul check-coverage --config .istanbul.yml",
    "upload-coverage-coveralls": "cat ./coverage/lcov.info | coveralls",
    "watch-lint": "esw --watch .",
    "watch-test": "yarn run test -- --watch --reporter min",
    "snyk-protect": "snyk protect",
    "prepublish": "yarn run snyk-protect",
    "precommit": "yarn run git-hooks",
    "postrewrite": "yarn run git-hooks",
    "git-hooks": "yarn run lint && yarn run generate-coverage && yarn run check-coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nhsuk/bunyan-logger.git"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bunyan": "^1.8.10",
    "is-nan": "^1.2.1",
    "snyk": "^1.29.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "coveralls": "^2.13.0",
    "eslint": "^3.19.0",
    "eslint-config-airbnb": "^14.1.0",
    "eslint-plugin-import": "^2.2.0",
    "eslint-plugin-json": "^1.2.0",
    "eslint-plugin-jsx-a11y": "^4.0.0",
    "eslint-plugin-mocha": "^4.7.0",
    "eslint-plugin-react": "^6.7.1",
    "eslint-watch": "^3.1.0",
    "husky": "^0.13.3",
    "istanbul": "^0.4.5",
    "mocha": "^3.3.0"
  },
  "snyk": true
}
